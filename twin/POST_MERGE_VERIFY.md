# Post-merge Twin verification

This file is an immutable audit marker for the canonical main verification hook.

The Twin Bridge workflow executes on every push to main, generates independent A/B attestations, and fails closed unless the equivalence gate passes.
