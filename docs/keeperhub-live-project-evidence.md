# KeeperHub Main Track — Live Project Evidence

Status: `LIVE_PROJECT_PASS / INTEGRATION_DEPTH_PENDING_LIVE_EXECUTION`

Observed on 2026-09-13 through the owner's Vercel account and direct deployment readback.

## Live product

**RUMBO Agent Control — Agent Reliability & Control Plane**

Canonical public production domain observed:

`https://rumbo-agent-control.vercel.app`

Vercel project:

- project name: `rumbo-agent-control`
- project id: `prj_oybRUYuAqDn7m1YRjZfCnuoDK4ty`
- team: `agent ai ingenieria`
- project created: 2026-08-26 22:48:23 UTC / 19:48:23 ART
- production deployment state at verification: `READY`
- production deployment observed: `dpl_9q22kCXev9YGC5zbLFED5RcfrwHn`
- stable production domain returned HTTP 200 at verification time.

The project and initial production deployment predate the KeeperHub Agent Economy build phase, which begins 2026-09-06.

## What the deployed product demonstrates

The live page identifies itself as `RUMBO Agent Control — Agent Reliability & Control Plane` and exposes a public commercial MVP centered on:

- explicit authority;
- requested/executed/successful/verified separation;
- canonical state;
- execution receipts;
- verification and promotion gates;
- bounded autonomy levels;
- an interactive control-plane demo;
- an Agent Control Readiness Score;
- a public Agent Control Audit offer.

The page explicitly labels demo metrics as sample data and does not claim client traction, certification, or guaranteed ROI.

## Relationship to this repository

`verifiable-agent-control-plane` is the public, sanitized reference extraction for the reliability/control-plane patterns used by larger agent systems. Its README explicitly says it is not a private production-control-plane dump.

The KeeperHub adapter added to this repository is the public integration component for the RUMBO Agent Control product lane. The submission must keep the boundary precise:

- it MAY claim RUMBO Agent Control is a pre-existing deployed product;
- it MAY claim this repository contains the audited public KeeperHub integration component;
- it MUST NOT claim that the current Vercel frontend imports this Python package directly unless that deployment relationship is separately observed;
- it MUST NOT claim live KeeperHub execution until an execution ID, terminal readback and transaction proof exist.

## Hackathon eligibility mapping

The event definition accepts a live project with users, **a deployed product**, or an active protocol. RUMBO Agent Control satisfies the deployed-product branch through the pre-hackathon production deployment above.

Therefore:

`EXISTING_PROJECT_PROVENANCE=PASS`

`LIVE_PROJECT_DEPLOYED_PRODUCT=PASS`

`KEEPERHUB_CODE_INTEGRATION=PASS`

`KEEPERHUB_LIVE_EXECUTION=NOT_YET_PROVEN`

`MAIN_TRACK_SUBMISSION=NO_GO_UNTIL_EXECUTION_TX_VIDEO`

## Remaining falsification gate

The strongest remaining challenge is not whether the project is live. It is whether KeeperHub is demonstrably the execution layer for value movement that RUMBO Agent Control triggers, consumes, or benefits from.

That gate requires real external evidence: authenticated KeeperHub access, an owned workflow, one bounded execution, terminal readback, transaction proof and the demo video.
